# Loosely pinned on purpose: Kaggle's base image wins, and fighting it costs
# more than it saves. Lower bounds are the versions that actually matter:
# Qwen2-VL support landed in transformers 4.45, and 8-bit paged optimisers
# need a recent bitsandbytes.

# --- core ML ---------------------------------------------------------------
torch>=2.1
transformers>=4.45.0
peft>=0.13.0
bitsandbytes>=0.44.0
accelerate>=0.34.0
trl>=0.11.0
qwen-vl-utils>=0.0.8

# --- data ------------------------------------------------------------------
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # parquet cache for the CSV loader
pillow>=10.0
pyyaml>=6.0

# --- utilities -------------------------------------------------------------
tqdm>=4.65
matplotlib>=3.7
scikit-learn>=1.3

# --- testing ---------------------------------------------------------------
pytest>=7.4
